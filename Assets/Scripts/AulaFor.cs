using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AulaFor : MonoBehaviour
{

    // For é um loop/laço de repetição

    /* Laço (loop) de repetição é uma estrutura que executa 
     * um conjunto e instruções enquanto uma determinada 
     * condição é verdadeira */

    // Start is called before the first frame update
    void Start()
    {
        // for(valorInicial; CondiçãoFinal; ValorIncremento)
        for ()
        {

        }
    }
}
